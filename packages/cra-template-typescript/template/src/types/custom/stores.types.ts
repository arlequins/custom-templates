export const enum SliceEnum {
  DASHBOARD = 'dashboard',
}

export const enum AuthThunkEnum {
  DASHBOARD_LIST = 'dashboard/list',
}

export const enum StatusEnum {
  LOADING = 'loading',
  IDLE = 'idle',
  FAILED = 'failed',
  INIT = 'init',
}
